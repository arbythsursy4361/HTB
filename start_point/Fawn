The acronym FTP stand for?
Ans:file transfer protocal

Which port does the FTP service listen on usually?
Ans:21
[You can input $nmap -Pn (IP), then you will find out all ports.]

What acronym is used for the secure version of FTP?
Ans:sftp
["Secure" File Transfer Protocol.]

What is the command we can use to send an ICMP echo request to test our connection to the target?
Ans:ping

From your scans, what version is FTP running on the target?
Ans:vsftpd 3.0.3
[-A: Enable OS detection, version detection, script scanning, and traceroute.]

From your scans, what OS type is running on the target?
Ans:Unix
[As mentioned above.]

What is the command we need to run in order to display the 'ftp' client help menu?
Ans:ftp -h/-?

What is username that is used over FTP when you want to log in without having an account?
Ans:anonymous
[We can use "anonymous" as username which is already covered in previous task and password field try default value i.e. "password".]

What is the response code we get for the FTP message "Login successful"?
Ans:230
[As mentioned above.]

There are a couple of commands we can use to list the files and directories available on the FTP server, One is dir, and the other is?
Ans:ls

What is the command used to download the file we found on the FTP server?
Ans:get
-----
Finally, you can input exit to leave ftp command.
Then, using $ls to observe the download file i.e. "flag.txt".
the result which using $cat command to get the content of flag.txt.
